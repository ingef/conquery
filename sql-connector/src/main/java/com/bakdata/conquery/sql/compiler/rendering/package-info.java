/**
 * Internal services that render compiler IR into database-specific SQL expressions.
 *
 * <p>Backend references are temporary while the remaining legacy compiler transformations move into the SQL
 * connector.</p>
 */
package com.bakdata.conquery.sql.compiler.rendering;
