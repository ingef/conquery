/** Marks something as unread. Decorative: the owner names the state for screen readers. */
export const Dot = () => (
  <span
    aria-hidden
    className="block size-2 shrink-0 rounded-full bg-primary-500"
  />
);
