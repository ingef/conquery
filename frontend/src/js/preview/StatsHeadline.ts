import styled from "@emotion/styled";

export const StatsHeadline = styled("h3")`
  font-size: ${({ theme }) => theme.font.sm};
  color: ${({ theme }) => theme.col.black};
  font-weight: 400;
  margin: 14px 0 0;
`;
