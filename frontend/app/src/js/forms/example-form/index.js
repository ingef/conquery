// @flow
import { default as ExampleForm } from './ExampleForm';

const exampleForm = {
  type: "EXAMPLE_FORM",
  component: ExampleForm,
  reducer: (state) => state || {}
};

export default exampleForm;
