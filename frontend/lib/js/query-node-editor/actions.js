// @flow

// import { createActionTypes } from './actionTypes';

export const createQueryNodeEditorActions = (type: string): Object => {
  // In the future: use QueryNodeEditor-internal action types here
  // const actionTypes = createActionTypes(type);

  return {};
};
