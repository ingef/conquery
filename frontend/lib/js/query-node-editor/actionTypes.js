// @flow

// import { toUpperCaseUnderscore } from '../common/helpers';

export const createActionTypes = (type: string) => {
  // In the future: define QueryNodeEditor-internal actions here
  // const uppercasedType = toUpperCaseUnderscore(type);
  return { };
};
