package com.bakdata.conquery.io.external.form;

public enum DatasetDetail {
	STATISTIC, // Produce only results which are aggregations over analysed groups
	FULL // Include entity detail in the report
}
